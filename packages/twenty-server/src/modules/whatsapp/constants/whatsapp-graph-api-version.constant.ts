// The API version the Meta test app was verified against via curl (see task notes).
// Bump deliberately, not implicitly, since Graph API versions are deprecated on a schedule.
export const WHATSAPP_GRAPH_API_VERSION = 'v21.0';
